from setuptools import setup

setup(name='falcon_versioning',
      version='1.0.0',
      long_description=open('README.md', 'r').read(),
      license='MIT License',
      packages=['falcon_versioning']
      )
